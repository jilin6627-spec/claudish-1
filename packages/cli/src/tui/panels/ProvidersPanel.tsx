/** @jsxImportSource @opentui/react */
import { C } from "../theme.js";

/** Deprecated standalone panel: custom endpoints form integrated natively into ApiKeysPanel. */
export function ProvidersPanel() {
  return (
    <box padding={2}>
      <text><span fg={C.dim}>This pane has been cleanly merged into Api Keys.</span></text>
    </box>
  );
}
